import { defineConfig } from 'astro/config';

// Root GitHub Pages site at https://geparada.github.io
// If you ever switch to a project site (e.g., personal-website), add:
//   base: '/personal-website'
// and update internal links accordingly.
export default defineConfig({
  site: 'https://geparada.github.io',
});
